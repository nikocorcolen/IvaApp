<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IvaApp.Pages.BuyAndSellP">
  <ScrollView>
    <StackLayout Padding="40" Spacing="20">
      <Button x:Name="registryBuyButton"
              Text="Registrar comrpa"
              TextColor="White"
              BackgroundColor="#77D065"/>
      <Button x:Name="registrySellButton"
              Text="Registrar venta"
              TextColor="White"
              BackgroundColor="#77D065"/>
      <Button x:Name="simulateBuyButton"
              Text="Simular compra"
              TextColor="White"
              BackgroundColor="#77D065"/>
      <Button x:Name="simulateSellButton"
              Text="Simular venta"
              TextColor="White"
              BackgroundColor="#77D065"/>
      <Button x:Name="ivaLabel"
             Text=""
             Font="24"
             HorizontalOptions="Center"
             TextColor="White"
             BackgroundColor="Transparent"/>
    </StackLayout>
  </ScrollView>
</ContentPage>